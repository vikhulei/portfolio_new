import styled from "styled-components"
import { colors } from "../../util/Variables"

const {lightbrown, middlebrown, darkbrown} = colors

const MyName = styled.p `
    font-size: 2rem;
`

const MyImageContainer = styled.div `

`

const MyImage = styled.img `

`

 